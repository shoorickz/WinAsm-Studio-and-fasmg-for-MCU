This package contains reduced set of WinAsm Studio and other tools optimized for programming in assembly for various MCU/CPU easy and efficiently.
You can start to use it immediately after placing in C:\MCU folder. Other location is possible but will require reconfiguring (usually via ini-files edition).
